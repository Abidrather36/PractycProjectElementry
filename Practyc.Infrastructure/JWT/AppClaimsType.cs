namespace Practyc.Infrastructure.JWT
{
    public class AppClaimsType
    {
        public const string UserId =nameof(UserId);

        public const string Email = nameof(Email);

        public const string UserName = nameof(UserName);

    
    }
}
